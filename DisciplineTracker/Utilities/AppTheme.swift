import SwiftUI

enum AppTheme {
    static let background = Color(hex: "0D0B16")
    static let surface = Color(hex: "161426")
    static let border = Color(hex: "2E2A4D")
    static let accent = Color(hex: "8B7CFF")
}

